"""
.. todo::
    Document models and views. Unfortunately, Django's models do not play well
    with the autodocs generator and will need some custom code.

    Django's models are largely self-explanatory, and the code base contains
    help texts for every attribute which should be automatically pulled into
    the documentation.

"""